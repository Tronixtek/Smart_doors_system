# Local device development can still use adb reverse with the default localhost fallback.
# For cloud-backed builds, point this to your deployed API.
EXPO_PUBLIC_API_URL="https://api.smartdoor.example.com/api/v1"
